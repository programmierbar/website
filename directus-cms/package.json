{
  "name": "directus-cms",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "migrate:db": "directus database migrate:latest",
    "bootstrap": "directus bootstrap",
    "build": "yarn workspace shared-code build && rm .env && yarn migrate:db && yarn bootstrap",
    "start": "directus start",
    "snapshot-schema": "directus schema snapshot ./schema.json --format json",
    "apply-schema": "directus schema apply ./schema.json"
  },
  "dependencies": {
    "@slack/web-api": "^6.5.1",
    "axios": "^0.24.0",
    "directus": "9.23.1",
    "jsonwebtoken": "^8.5.1",
    "shared-code": "0.0.0"
  },
  "devDependencies": {
    "prettier": "^2.5.1"
  }
}
